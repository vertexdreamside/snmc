# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# QR token signing secret (HMAC) — generate a long random string, never commit the real value
QR_SIGNING_SECRET=
